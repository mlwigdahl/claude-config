# Server configuration
PORT=5001
NODE_ENV=development

# Client configuration
CLIENT_URL=http://localhost:3000

# File system security
# Comma-separated list of allowed base paths for file access
# If not set, defaults to user's home directory
ALLOWED_BASE_PATHS=/Users/username/projects,/Users/username/Documents

# Example for production (restrict to specific directories)
# ALLOWED_BASE_PATHS=/var/www/projects,/opt/app/data